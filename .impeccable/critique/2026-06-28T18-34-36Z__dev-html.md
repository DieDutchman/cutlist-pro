---
target: dev.html
total_score: 34
p0_count: 0
p1_count: 0
p2_count: 2
p3_count: 3
timestamp: 2026-06-28T18-34-36Z
slug: dev-html
---
### Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | Autosave chip, price stale banner, room counts, dirty cutlist state, aria-live on total — comprehensive |
| 2 | Match System / Real World | 4 | Genuine cabinet-making vocabulary; domain-specific room names appropriate for target locale |
| 3 | User Control and Freedom | 3 | Per-row panel revert + per-section revert + modal cancels + Escape; no global session undo |
| 4 | Consistency and Standards | 3 | Amber system cohesive; SAVE JOB button + autosave chip sends mixed signals |
| 5 | Error Prevention | 3 | Delete confirmation, archive/delete separation, autosave; no dimension validation |
| 6 | Recognition Rather Than Recall | 3 | More hint auto-opens on first visit with pulse ring; pin affordance near-invisible |
| 7 | Flexibility and Efficiency | 4 | Alt+1–5, Alt+R, ? legend, batch mat, sort modes, tab pinning = strong customization |
| 8 | Aesthetic and Minimalist Design | 3 | Dark amber theme purposeful; job action bar carries 12+ equal-weight items |
| 9 | Help Users Recover from Errors | 4 | Panel revert uses pre-edit snapshot baseline correctly; section revert; autosave |
| 10 | Help and Documentation | 3 | ? modal, edge code legend, CSS tooltips, scenarios hint; no guided onboarding |
| **Total** | | **34/40** | **Good — solid foundation, two P2s to address** |

### Anti-Patterns Verdict

**LLM assessment**: Not AI-generated. Dark charcoal/amber palette, Oswald/Barlow/JetBrains Mono triple-font system, function-first tab navigation purpose-built for a specific professional domain. No gradient text, no glassmorphism, no hero metrics. Tab pinning (📍📌) adds personality without breaking the system.

**Deterministic scan**: 134 raw findings. After FP removal: 126 advisories (124 color + 2 radius), 0 warnings. Net new actionable: `border-radius: 16px` on `.pp-vat-select` (line 3459), `border-radius: 8px` on feedback dot (line 4483).

### Priority Issues

**[P2] Tab pin discoverability: the feature is invisible**
- 📍 button at opacity:0.3 default (0.4 in panel), no tooltip explaining pin concept, no first-run callout
- Fix: opacity 0.6 default + `[data-tip]="Pin to main bar"` on pin span + optional one-shot tip after More auto-open
- Command: `/impeccable onboard`

**[P2] Job action bar cognitive overload**
- 12+ items at uniform visual weight; Delete adjacent to Save/New; working memory > 4 items
- Fix: zone grouping with separators: [job actions] | [danger zone] | [status] | [account]
- Command: `/impeccable layout`

**[P3] border-radius outside documented scale (detector caught)**
- `.pp-vat-select` at 16px (line 3459), feedback dot at 8px (line 4483). Scale max is pill:10px
- Fix: `10px` or `50%` on both
- Command: `/impeccable polish`

**[P3] Batch material select missing aria-label**
- Fix: add `aria-label="Set all panel materials to"` (one-liner)
- Command: `/impeccable polish`

**[P3] Pinned tab missing aria-controls**
- _applyTabPins sets role="tab" but not aria-controls on moved tabs
- Fix: add aria-controls in _applyTabPins or verify original tabs carry the attribute
- Command: `/impeccable polish`

### Persona Red Flags

**Alex**: Tab pinning is mouse-only multi-step; no bulk panel row operations; Alt+1–5 doesn't follow pinned layout.
**Jordan**: More hint race condition (click anywhere during 1.5s delay = hint dismissed); job bar overwhelming; no onboarding flow; primary workflow (Tools row) below the fold of the first tab row.
**Sam**: Batch mat select nameless; pinned tabs lack aria-controls; pp-vat-select custom caret relies on appearance:none.
**Nico**: Tab pinning perfect for his workflow but unDiscoverable; no scenario A/B/C diff still missing.

### Minor Observations
- closeMoreMenu() in toggleTabPin = good but requires reopening More to pin multiple tabs
- Barlow/Oswald detector warnings at 6863-6864 are FPs; add to ignore.md
- 124 color advisories: rgba drift continues post-documentation; run a DESIGN.md sweep

### Questions to Consider
- "The job action bar is doing the work of a menu bar. What if archive/delete lived behind a ⋯ overflow, leaving only Save/New/Plan in the primary row?"
- "Tab pinning is discovered by accident. What if More dropdown had a brief in-menu callout on first open?"
- "Alt+1–5 maps to fixed tabs, not toolbar order. Should shortcuts follow the pinned layout?"
