name: 🚀 Promote DEV → LIVE

# ONLY runs when you manually trigger it from the GitHub Actions tab.
# Copies dev.html → index.html and deploys the live site.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type PROMOTE to confirm going live'
        required: true
        default: ''

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - name: Check confirmation
        run: |
          if [ "${{ github.event.inputs.confirm }}" != "PROMOTE" ]; then
            echo "❌ Type PROMOTE in the input box to confirm."
            exit 1
          fi
          echo "✓ Confirmed — promoting dev.html to live"

      - name: Checkout repo
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Copy dev.html → index.html
        run: |
          if [ ! -f "dev.html" ]; then
            echo "❌ dev.html not found in repo root"
            exit 1
          fi
          cp dev.html index.html
          echo "✓ dev.html → index.html"

      - name: Commit and push index.html
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "ℹ index.html already matches dev.html — nothing to commit"
          else
            TIMESTAMP=$(date '+%Y-%m-%d %H:%M UTC')
            git commit -m "🚀 Promote dev.html → index.html [$TIMESTAMP]"
            git push
            echo "✓ index.html committed and pushed"
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

      - name: Done
        run: |
          echo ""
          echo "✅ LIVE SITE UPDATED"
          echo "   https://diedutchman.github.io/cutlist-pro/"
